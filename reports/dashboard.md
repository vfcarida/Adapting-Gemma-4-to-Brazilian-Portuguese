# Results Dashboard

> This file is generated by `scripts/build_dashboard.py` after a real
> evaluation run (`gemma4pt eval` populates `reports/eval_results.json`,
> which this script reads). No model has been trained or evaluated yet in
> this checkout, so there is nothing to report — the version of this file
> previously committed here contained hand-written placeholder numbers
> presented as if they were real results; that has been removed.
>
> Run `gemma4pt train-cpt configs/train/cpt_pilot.yaml` (or the Colab
> notebook under `colab/`), then `gemma4pt eval` and
> `python3 scripts/build_dashboard.py --format markdown` to regenerate this
> file with actual numbers.
