# =============================================================================
# Requirements — Gemma 4 Portuguese Adaptation
# =============================================================================
# This file mirrors pyproject.toml dependencies for environments where
# pip install -e . is not practical (e.g., Docker, CI without editable install).
# Keep in sync with pyproject.toml [project.dependencies] and optional-deps.
# =============================================================================

# ── Core ML ──────────────────────────────────────────────────
torch>=2.6.0
# Gemma 4 (Gemma4ForConditionalGeneration) requires transformers>=5.5.0.
transformers>=5.5.0
datasets>=3.0.0,<4.0.0
accelerate>=1.2.0
peft>=0.19.0  # Gemma4ClippableLinear (vision-encoder freeze path) support landed here
trl>=1.0.0
sentencepiece>=0.2.0
protobuf>=4.25.0

# ── Data quality ─────────────────────────────────────────────
# datasketch>=2.0.0 changed the default MinHash permutation scheme
# ("affine32"); rebuild any persisted sketches/LSH indexes after upgrading.
datasketch>=2.0.0
xxhash>=3.4.0
nltk>=3.8.0
jsonlines>=4.0.0

# ── Evaluation ───────────────────────────────────────────────
scipy>=1.12.0
scikit-learn>=1.4.0
numpy>=1.26.0
tabulate>=0.9.0

# ── Config / CLI / env ───────────────────────────────────────
pyyaml>=6.0.1
python-dotenv>=1.0.0
rich>=13.7.0
typer>=0.12.0
tqdm>=4.66.0

# ── Dev (also in pyproject.toml [dev]) ───────────────────────
pytest>=8.0.0
pytest-socket>=0.7.0
ruff>=0.11.0

# ── GPU (install separately: pip install -r requirements-gpu.txt) ──
# See pyproject.toml [project.optional-dependencies.gpu]
# bitsandbytes>=0.45.0
# deepspeed>=0.15.0
# flash-attn>=2.7.0

# ── Eval engine (install separately: pip install -r requirements-eval.txt) ──
# See pyproject.toml [project.optional-dependencies.eval]
# vllm>=0.8.0
# bert-score>=0.3.13

# ── Monitoring (install separately or use pip install -e ".[monitoring]") ──
# wandb>=0.18.0
# tensorboard>=2.16.0
